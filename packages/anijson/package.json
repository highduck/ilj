{
  "name": "@highduck/anijson",
  "version": "0.0.1",
  "main": "dist/commonjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "ci": "run build",
    "build": "run build:esm && run build:commonjs",
    "build:esm": "tsc -p tsconfig.esm.json",
    "build:commonjs": "tsc -p tsconfig.commonjs.json",
    "clean": "rimraf dist"
  },
  "sideEffects": false,
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^3.9.5"
  }
}
