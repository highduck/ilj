{
  "name": "@highduck/build-config",
  "version": "0.0.1",
  "scripts": {
    "ci": "run build && run test",
    "build": "tsc -b tsconfig.project.json",
    "watch": "tsc -b -w tsconfig.project.json",
    "clean": "rimraf ./dist/",
    "test": "jest"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.js",
  "type": "module",
  "sideEffects": false,
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.5.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  }
}
